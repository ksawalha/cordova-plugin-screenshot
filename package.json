{
  "name": "cordova-plugin-screenshot",
  "version": "1.0.0",
  "description": "Cordova plugin to capture a screenshot of the current view with full height.",
  "main": "www/screenshot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cordova",
    "plugin",
    "screenshot",
    "android"
  ],
  "author": "Your Name",
  "license": "MIT",
  "cordova": {
    "id": "cordova-plugin-screenshot",
    "platforms": [
      "android"
    ]
  },
  "dependencies": {
    "cordova-android": "^9.0.0"  // Adjust this version based on the Cordova Android version you are targeting
  }
}
